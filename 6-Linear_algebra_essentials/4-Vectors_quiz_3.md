$$
   x =
      \begin{bmatrix}
       a \\
       b
      \end{bmatrix}


$$

$$
\alpha \in \mathbb{R}
$$

$$
||\alpha \cdot \vec{x}|| =
\alpha \cdot  \sqrt{a^2 + b^2}
= \alpha \cdot  ||\vec{x}||
$$

(b) No, the direction of the vector x keeps the same for a positive scalar.

(c) If the scalar is negative the direction of the vector x will be shifted by 180 degrees.

(d) If the modulo of the scalar is less than one, the magnitude of $$||\alpha \cdot \vec{x}||$$
will decrease.
